# wealth-tensor — THE CORPUS CHECKLIST (session-0, retrofitted at wealthTensor-52½)
<!-- Written 2026-08-16 by the sessionZero process audit (Jason + Fable, interactive), fifty-two
     sessions late and therefore exactly on time. CONTOURS, NOT PRESCRIPTIONS: each line says
     what done looks like; the how is the session's. The gate prints distance-to-done at every
     orient. P-numbers, deliberately NOT C-numbers — C## belongs to CONSTRAINT-INVENTORY-001.
     THE CHARTER STILL WINS; ADR-001 still governs sequencing; this file adds no new law, it
     makes the existing law COUNTABLE. -->

## The destination (ADR-001, restated once)
Three preprints — II, III, IV — each at "ready to submit", then ONE batch. Nothing ships early.

- [ ] P1 — **Paper III measured against its ADR-001 DoD clause + PREPRINT-CHECKLIST §A**, gap
      by gap, results appended to THIS file as checkable sub-lines. (This is the standing §3
      at-bat: measure, don't characterise. Nobody has done it. It is the next swing.)
- [ ] P2 — **Paper III gaps closed in the prose, in the file** (WT-079: the deliverable is the
      paper, not a list of fixes). Straw men live in paper-III.md, not in tickets.
- [ ] P3 — **Paper II re-measured** against the same two lists (it built the template; confirm
      it still satisfies it), gaps closed → ready to submit.
- [ ] P4 — **Paper IV exists as a full draft** — its own charter plus Paper I's surviving
      subsection (the crossing height IS the volume) and the Abandoned Approaches ledger.
      The long pole; nothing posts until it stands.
- [ ] P5 — **Paper IV → ready to submit** (same two lists, same bar).
- [ ] P6 — **Every number in every paper regenerates from a COMMITTED script at the pinned
      SHA**, re-run at close — a number without a script is a number nobody has checked since
      the day it was typed (PREPRINT-CHECKLIST, the WT-027 lesson).
- [ ] P7 — **Convergence, per paper — this is what "perfection == done" means here:** fresh-eyes
      referee passes (REVIEW-NNN docs, opposing-team charter) repeat until TWO CONSECUTIVE
      passes produce ZERO substantive findings. Not flawlessness — someone, somewhere, will
      always find something; convergence + coverage is the honest, countable stand-in. A pass
      that finds nothing is a RESULT and gets its REVIEW doc like any other.
- [ ] P8 — **Jason's own-hand pass over each converged paper** — his voice, his name, the last
      human step. The corpus is not "ready to submit" until his hand has been through it; every
      session before this line exists to make this step small instead of dreadful.
- [ ] P9 — **The batch moment**: all three at terminal state → say so ONCE, plainly. Never ask
      Jason to trigger a submission (ADR-001 `-08`); declaring readiness is the session's job,
      submitting is his.
- [x] P10 — **Registered in the shared ledger with a calibrated estimate** (2026-08-16, at
      retrofit — the brake now counts what sixty sessions never counted).

## The guard contour (the drift that ate ten sessions, named)
The guard track is PAUSED (Jason, 2026-08-15) and STAYS paused as a default. A new guard is
in-contour only when it names the paper claim it protects and that claim sits on an open P-line
— "which paper sentence does this guard keep true?" is the question ten sessions never asked.
The 61 probes already built are an asset; they are not a destination.
