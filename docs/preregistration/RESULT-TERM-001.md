# RESULT · TERM-001 THE RECTANGLE PAPER III CALLS "DISCLOSED" IS THE ONE 86.1 % OF THE DISCLOSURE FALLS OUTSIDE

*wealthTensor-37 · 2026-08-14 · the eighth costume of the §1.3 shape, and the last pre-written paste on the pile — seven sessions old. The stanza was written by `wealthTensor-31`, carried unchanged through `-32` … `-36`, and it names **four** sites. There are **five**. Registered below **after every site was read in its own context** and **before any anchor was touched**, and committed alone. State Machine `1217494219393416` is a different question and is not this.*

---

## 1 · The find

Paper III's §4.4 sweeps a product of assumed useful lives — ten to forty years for property, three to twenty for finite-lived intangibles — and calls it, at five sites across four sections, *the disclosed rectangle*. `RESULT-REG-009` §3 measures the disclosure against that product directly: **S = 0.1391**, ninety-five of six hundred and eighty-three disclosed firm-year pairs fall inside it. **86.1 % of the disclosure lies outside the rectangle named after it.** The adjective states the opposite of the measurement, and it is the manuscript's own measurement, printed in §4.4 six lines above the first offending site.

**`wealthTensor-30` already repaired this, and stopped two sites short of the section it was repairing.** `REG-009` §12 registered the repair in advance and named its two surfaces — §4.4's 99.7 % sentence and §7's admissibility row — and both were done: §4.4's sweep sentence now says *"the rectangle those lives were **assumed** to span"*, and §7's row is titled *"The **asserted** rectangle lies outside the model's domain"*. The straggler is in the very next paragraph, carrying the same section's argument: §4.4 says *"the entire **disclosed** rectangle lies outside the domain"* at the calibrated rate and then, **five lines later and in the same paragraph**, *"the **asserted** rectangle lies inside the domain after all"* at the measured one. One object, two names, one paragraph, either side of the paper's sharpest reversal. The remaining three sites are §4.9's two and §4.10's one, and the fifth is §7's own test column — a *different cell of the same table* whose claim column `-30` repaired.

**Why the pre-written stanza says four.** It was written against the file at `958956a~1` and named line numbers: 964, 996, 1123, 1573. Those four sites are live and are still four of the five — they now sit at 976, 1008, 1135 and 1591, drifted by twelve, twelve, twelve and eighteen. **The site the stanza omits is the §4.4 one, which is the only one of the five that is *inside* the section `-30` was repairing and is therefore the one whose survival is a finding rather than an oversight.** A pre-written repair is an unverified claim whose author had less context than the session performing it (`-35`), and this is that shape in its cheapest form: the citation was right, the coordinates were stale but recoverable, and the **count was wrong** — and the count is the only part of the stanza that no anchor would have contradicted, because a patch that resolves four anchors exactly once reports total success while a fifth site sits untouched two hundred lines away.

**Why nothing caught it.** The adjective carries a claim about the disclosure and lives only in prose; no instrument emits it, no ledger key contains it, and `test_ledger_provenance`'s key for the §7 row was deliberately anchored on `"rectangle lies outside"` **with the adjective omitted** — a decision recorded in that file's own comment, taken after `-30`'s rename went stale, and exactly right for what it guards. A key that survives a rename cannot also detect one. Nothing else in `scripts/`, `tests/` or `src/` has ever known that this word is supposed to agree with a number in `RESULT-REG-009`.

## 2 · The repair, registered before it was performed

**Registered before the edit: five sites, every one of them read in its own context first, all five replaced by the term already registered for this object.** The ruling `REG-009` §12 fixed — the rectangle is labelled *asserted* rather than observed — is adopted unchanged and simply carried to the sites it did not reach. `wealthTensor-31`'s stanza is adopted in substance and **amended in exactly one respect: the count is five, not four**, and the fifth is named above.

**The referent test, applied to each site before the count was written down.** A site is in scope only where *rectangle* denotes the assumed product [10, 40] × [3, 20]. At §4.4 the object is the product the preceding sentence says those lives were *assumed* to span; at §4.9 twice and §4.10 once it is the span from a forty-year to a three-year life, whose fast edge δ = 0.333 the surrounding text uses by name; at §7 it is the row testing three recognition rates across that same grid. **No site denotes the 683 disclosed pairs, and had one done so it would have stayed *disclosed* and the count would have been four** — which is stated here because the branch was live when the sites were read and the count is a finding, not a transcription.

**What is NOT touched, and why each exclusion is a rule rather than a convenience.** The eleven occurrences of the bare noun *rectangle* keep their wording: the defect is the adjective's claim, not the noun, and rewriting unclaimed nouns is how a terminology pass turns into a reflow. **The registration and result documents that use the old phrase are records and stay records** — `RESULT-REG-004`, `RESULT-REG-003`, `RESULT-REG-002`, `RESULT-P0`, `REG-003`, `REG-004` and `SOURCE-001` said what they said when they said it, and `SOURCE-001` is finished by standing ruling. **The instrument prose in `scripts/` stays too**: `wt088_disclosed_ladder.py` and its four siblings print the phrase inside severity-check descriptions and run logs that other documents quote and pin, and a cosmetic rename there would edit the witness to match the testimony. This document is the disclosure of that inconsistency, which is the honest form of it.

**Five anchors, every one a span with no internal newline, no heading or rule inside any of them, and the structure delta declared empty.** No claim is weakened, no claim is strengthened, no hedge is added, no free parameter is introduced, nothing is absorbed as a caveat, and no section outside §4.4, §4.9, §4.10 and §7 is touched. G-COACH-3 is evaluated across the pass with `defensive_count.py --against` on a copy of the pre-edit manuscript taken **before the tree was dirtied**, and the full suite is run after.

**The guard binds the adjective to the measurement, not to taste, and it fails in both directions.** `tests/test_term001_rectangle.py` imports every string, every builder and the edit list itself from `scripts/wt101_edits_term001.py` — the one place in the repository where any of them is written down — and asserts four things. That the wrong phrase occurs **zero** times in paper III, so a reintroduction anywhere goes red. That each repaired span occurs **exactly once**, so a later reflow that dissolves an anchor is visible rather than silent. That the **site count this document states equals the length of the edit list**, so a sixth site added without amending this paragraph goes red and an amended numeral with no sixth edit also goes red — the numeral has one parse and the list is it. And, the one that makes the rename *true* rather than merely consistent, that **the share this manuscript prints as falling outside the rectangle is the complement of `RESULT-REG-009`'s measured S**, recomputed from that document at test time: if the measurement is ever removed, restated or drifts, the adjective loses its warrant and the guard says so. The list cannot be emptied to make any of this pass — an empty edit list is asserted red, because a scan that finds nothing must fail rather than report coverage over nothing.

**What this does not do.** It does not re-edge the band count or re-read its floor; `CONSTRUCTION-REG-009-coverage-fill` R5 forbids it and card `1217494219393416` is the legitimate form of that question. It does not merge REG-010's population with the band count's. It does not promote either banding or the mirror. It does not re-score P3, which failed. It does not repair §4's coverage silence or §7.5's two errata, which stay recorded by standing ruling, and it does not touch §10, restore §11's global pin, promote `R_MIN` or spend the tie-break. And it does not claim that *asserted* is the last word on the object — only that the paper may not call a rectangle disclosed when its own §4.4 reports, six lines earlier, that 86.1 % of the disclosure falls outside it.
