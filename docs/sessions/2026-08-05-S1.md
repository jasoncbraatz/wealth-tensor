# S1 — 2026-08-05 · first session

Started as "can we work on a Google Doc together?" and ended with a repo, 30 tests, a
32-entry ledger and three new manuscript sections. Chronological, with paths.

## What was set out to do
Nothing, initially. The session began as a tooling question and became the project.

## What actually changed

**Infrastructure**
- Enabled 11 Workspace APIs on GCP project `1054330720958` via gcloud on darwin.
  Pre-change snapshot: `~/Desktop/downloads/gcp-1054330720958-services-BEFORE.txt` (23 services).
  Rollback: `gcloud services disable <api> --project=1054330720958`.
- Created public repo `github.com/jasoncbraatz/wealth-tensor`.

**Manuscript** — Google Doc `1RjAHJ7jHCX_N3ToHtstQvjlMb-BQz-iejBZ9Y4f58V8`
- Restore point taken and VERIFIED before any edit: doc `1fszgf295NGfvfQlT3VaCrRXrXA4K589tlS-bVsUIok4`
  (40,052 chars, 253 paragraphs, 4 tables). Rollback = open that copy.
- Removed 6 duplicate/orphan fragments (716 chars; arithmetic reconciled exactly).
- Added "Dimensional Status of the Transformation Efficiency Vector" (Lambda defence).
- Added the Georgescu-Roegen reconciliation paragraph.
- Added "Abandoned Approaches" section, seeded with one real entry.
- Added 8 paragraphs to the supply/demand section (SMD reframe, behavioural scope, order-book
  identity, nesting result). Added 6 references.
- Added "Layer, Not Error" to the Piketty chapter (11 paragraphs). Added 3 references.
- Added 4 paragraphs reframing the redistribution section from normative to positive; renamed
  "Policy Interventions for Stability:" to "Redistribution mechanisms represented in the
  modelling literature:".
- Doc grew 40,052 -> 59,580 chars. Every batch verified by independent read-back.

**Code** — 30 tests, all passing
- `src/wealth_tensor/cournot.py` + tests (11) — three mutually-checking solvers, corner handling.
- `src/wealth_tensor/excess_demand.py` + tests (9) — curves as two readings of one c(m).
- `src/wealth_tensor/lag.py` + tests (10) — reporting layer as a filter.
- `scripts/handoff_gate.py` — staleness guard for docs/HANDOFF.md.
- `docs/LEDGER.md` — 32 entries. `docs/HANDOFF.md` — this session's handoff.

**Corpus** — 4 global lessons banked to `claude-blackbook`, all auto-pushed.

## What was verified, not asserted
- `gate-selfcheck.sh`: PASS (0 uncommitted, 0 unpushed, all clone roots).
- pytest: 30 passed.
- Three subagent read-backs of the manuscript: 5/5, 6/6, 6/6.
- Restore point opened and measured (40,052 chars) rather than assumed.

## Defects found in our own work (the useful part)
1. Cournot closed form returned negative output for a priced-out firm. Fixed with iterative
   elimination — and the bug turned out to BE the manuscript's marginal pair (WT-001).
2. Eight inserted paragraphs silently rendered as H2. Caught by read-back, not by the API.
3. A full-width italic run was wiped by a later NORMAL_TEXT paragraph style.
4. An index computed by arithmetic off a cached snapshot was wrong by 6,944 chars. Caught by
   re-measuring. Would have written 4 paragraphs into the bibliography.
5. `variance_suppression` measured the wrong object; the corrected finding (volatility is
   relocated, not removed) is stronger than the original prediction (WT-028).
6. `handoff_gate.py --emit` refused its own first handoff because stamping moves HEAD. Fixed.

## Deferred
Checkbox-list question (WT-008), the twin document (WT-009), `redistribution.py` (WT-030),
the Lambda sensitivity sweep (WT-002 item 4), the citation-graph whitespace test (WT-006).
